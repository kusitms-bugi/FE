{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../path0 && npx pnpm@9.0.0 install --frozen-lockfile",
  "buildCommand": "cd ../path0 && npx pnpm@9.0.0 build:renderer && rm -rf src/renderer/renderer && cp -R dist/renderer src/renderer/renderer",
  "outputDirectory": "renderer",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
